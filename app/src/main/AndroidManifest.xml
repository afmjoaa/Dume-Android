<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.dume.dume">

    <!-- when a object needs internet to execute a job -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- if any object needs to read local storage -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".teacher.homepage.TeacherActivtiy" />
        <activity
            android:name=".splash.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".student.homepage.StudentActivity" />
        <activity
            android:name=".auth.AuthActivity"
            android:theme="@style/LoginTheme" />
        <activity android:name=".auth.code_verification.PhoneVerificationActivity" />

        <activity
            android:name=".auth.auth_final.AuthRegisterActivity"
            android:label="@string/title_activity_auth_final"
            android:theme="@style/MyStyle" />
        <activity
            android:name=".auth.social_init.SocialInitActivity"
            android:label="@string/title_activity_social_init"
            android:theme="@style/MyStyle"/>


<!--facebook fucking metadata-->
        <meta-data android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id"/>

        <activity android:name="com.facebook.FacebookActivity"
            android:configChanges=
                "keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>

    </application>

</manifest>